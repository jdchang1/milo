from setuptools import setup, find_packages

setup(
    name='milo',
    version='0.1.0',
    packages=find_packages(),
    description='Components for MILO: Model based Imitation Learning from Offline data',
)
